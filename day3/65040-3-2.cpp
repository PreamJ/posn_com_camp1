#include<stdio.h>
int main(){
	int max, min, i, a;
	for(i==0; i<10; i++){
		printf("Enter the number : "); scanf("%d", &a);
		if(max <= a) max = a;
		
		if(min >= a) min = a;
		
	}
	printf("The maximum numner is %d\n", max);
	printf("The minimum number is %d", min);
}
